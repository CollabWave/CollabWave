export default function DashboardLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <header>
          <div>I'm a new layout</div>
        </header>
        {children}
      </body>
    </html>
  );
}
